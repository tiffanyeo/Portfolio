/**
 * whyme.js
 * -----------------------------------------
 * Renders the "Why Me" section content.
 *
 * Responsibilities:
 * - Later: inject HTML into <section id="whyme">
 * - Later: handle any section-specific interactions or animations
 *
 * This file will be imported and called from main.js
 * via renderWhyMe().
 */

/**
 * renderWhyMe()
 * -----------------------------------------
 * Inserts the "Why Me" section markup.
 * Currently empty — will be implemented once
 * the HTML structure and text content are approved.
 */
export function renderWhyMe() {
    // TODO: Insert Why Me HTML into #whyme
    // Example (later):
    // const section = document.querySelector("#whyme");
    // section.innerHTML = `...`;
}
