/**
 * skills.js
 * -----------------------------------------
 * Renders the Skills section content.
 *
 * Responsibilities:
 * - Later: inject HTML into <section id="skills">
 * - Later: handle any interactive elements (filters, tags, etc.)
 *
 * This file will be imported and called from main.js
 * via renderSkills().
 */

/**
 * renderSkills()
 * -----------------------------------------
 * Inserts the Skills section markup.
 * Currently empty — will be implemented once
 * the HTML structure and text content are approved.
 */
export function renderSkills() {
    // TODO: Insert Skills HTML into #skills
    // Example (later):
    // const section = document.querySelector("#skills");
    // section.innerHTML = `...`;
}
